# Point the frontend at the live FastAPI backend. Leave empty to use the
# built-in verified mock dataset (fully interactive without a backend).
NEXT_PUBLIC_API_BASE=

# Intelligence dispatch / brief routing target.
NEXT_PUBLIC_DISPATCH_EMAIL=hello@storitellah.com
